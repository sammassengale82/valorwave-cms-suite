import { CapacitorConfig } from '@capacitor/core';

const config: CapacitorConfig = {
  appId: 'com.valorwave.cms',
  appName: 'ValorWave CMS',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;